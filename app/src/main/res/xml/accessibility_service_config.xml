<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately narrow: we only need enough to (a) dispatch gestures/text and
  (b) read the minimum node info required to find a requested UI element
  (e.g. "the search button"). No content-change or global-content-capture
  flags are set, per the spec's "لا تجمع بيانات غير ضرورية" requirement.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />
